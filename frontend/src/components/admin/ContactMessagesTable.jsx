const ContactMessagesTable = ({ messages, onMarkAsRead }) => <div className="space-y-3">{messages.map((message) => <article key={message._id} className={`rounded-2xl border bg-white p-5 ${message.isRead ? 'border-[#E0F2F1]' : 'border-[#5EEAD4]'}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="font-semibold text-gray-800">{message.name}</h2><a href={`mailto:${message.email}`} className="text-sm text-[#0F766E]">{message.email}</a></div><div className="flex items-center gap-3 text-xs text-gray-500"><time>{new Intl.DateTimeFormat('ar-DZ', { dateStyle: 'medium', timeStyle: 'short' }).format(new Date(message.createdAt))}</time>{!message.isRead && <span className="rounded-full bg-teal-50 px-2.5 py-1 text-teal-700">جديدة</span>}</div></div><p className="mt-4 whitespace-pre-wrap text-sm leading-6 text-gray-700">{message.message}</p>{!message.isRead && <button onClick={() => onMarkAsRead(message._id)} className="mt-4 text-sm font-medium text-[#0F766E] hover:underline">تحديد كمقروءة</button>}</article>)}{!messages.length && <div className="rounded-2xl border border-[#E0F2F1] bg-white p-8 text-center text-gray-500">لا توجد رسائل.</div>}</div>;
export default ContactMessagesTable;